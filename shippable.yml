language: node_js

node_js:
  - 0.12

build:
  pre_ci:
    docker build -t hongpham12/docker-centos-phamh:shippablev-v.0.0.1

integrations:
  hub:
    - integrationName: hongpham-dockerhub
      type: docker
